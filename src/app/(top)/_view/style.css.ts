import { style } from "@vanilla-extract/css";

const styles = {
    container: style({
        width: "100vw",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
    })
};

export default styles;
