import { CardView } from "@/components/view/cardView/view/cardView"
import { en } from "@/shared/lang/en"

export const Activity = async () => {
    return <CardView pageTitle={en.active.title} contents={[]} />
}
