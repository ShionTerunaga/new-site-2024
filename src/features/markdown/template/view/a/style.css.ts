import { style } from "@vanilla-extract/css";

const styles = {
    container: style({
        width: "100%",
        height: 120,
        marginTop: 4,
        border: "1px solid silver",
        borderRadius: 8,
        overflow: "hidden"
    })
};

export default styles;
