import { style } from "@vanilla-extract/css";

const styles = {
    codeBase: style({
        border: "1px solid silver",
        borderRadius: 4,
        padding: 4,
        color: "red"
    })
};

export default styles;
