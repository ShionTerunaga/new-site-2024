import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        display: "flex",
        alignItems: "center",
        marginTop: 10,
        marginBottom: 10
    })
}

export default styles
