import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        aspectRatio: "5/7",
        padding: 0,
        border: "none",
        backgroundColor: "transparent",
        cursor: "pointer",
    })
}

export default styles
