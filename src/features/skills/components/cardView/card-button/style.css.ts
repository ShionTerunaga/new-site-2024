import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        width: 202,
        height: 252,
        padding: 0,
        border: "none",
        backgroundColor: "transparent",
        cursor: "pointer"
    })
}

export default styles
