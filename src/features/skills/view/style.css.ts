import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        marginTop: 70,
        width: "100vw",
        height: "100vh",
        overflow: "auto"
    }),
    space: style({
        height: 70
    })
}

export default styles
