import { style } from "@vanilla-extract/css"

export const styles = {
    titleBox: style({
        width: 250,
        height: 200,
        fontSize: 26,
        textAlign: "center"
    })
}

export default styles
