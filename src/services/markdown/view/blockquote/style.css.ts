import { style } from "@vanilla-extract/css"

const styles = {
    container: style({
        borderLeft: "4px solid silver",
        color: "silver",
        marginLeft: 4
    })
}

export default styles
