import { style } from "@vanilla-extract/css"

const styles = {
    h1: style({
        paddingBottom: 5,
        paddingTop: 5
    }),
    hr: style({
        marginBottom: 5
    })
}

export default styles
